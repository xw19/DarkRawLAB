{
  "name": "darkraw-lab",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Mobile-first, browser-based RAW photo editor. Fully client-side.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "libraw-wasm": "^1.6.0",
    "twgl.js": "^6.2.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
